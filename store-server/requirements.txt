fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2
# androguard 3.x cannot parse resources.arsc from AGP 8 / AAPT2 >= 30 ("res1 must be zero")
androguard==4.1.2  # 4.1.3+ breaks on APK signing-block parsing (NoOverwriteDict.append)
# fdroidserver from PyPI (the official docker image is amd64-only)
fdroidserver>=2.3,<3
